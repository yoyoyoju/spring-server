package mock;

import org.springframework.data.jpa.repository.JpaRepository;

interface ValueRepository extends JpaRepository<Value, Long> {
    // Creating new instances
    // Updating existing ones
    // Deleting
    // Finding

}
